const error = () => {
  throw Error('Input has wrong format');
};

export class Converter {
  convert(numbers, from, to) {
    let calculation = 0;
    numbers.forEach((n, idx) => {

    });


  }
}

// the right most digit in FROM is the remainder of the division of n byb2